import { RegisterForm } from "../../../atoms/forms/RegisterForm/RegisterForm";

const RegisterSection = () => {
  return (
    <section className="MainSection" name="Login">
      <RegisterForm />
    </section>
  );
};

export default RegisterSection;
